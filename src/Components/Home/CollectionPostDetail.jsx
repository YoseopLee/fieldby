import React from "react"

const CollectionPostDetail = ({ postImages }) => {
    return (
        <div>
            <img src={postImages} alt=""/>
        </div>
    )
}

export default CollectionPostDetail;